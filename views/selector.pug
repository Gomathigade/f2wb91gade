extends layout

block content
  h1= title

  - image_names = ["a1-1", "a2-2", "a3-3", "a4-4", "a5-5"]

  - for(let i=0; i<3; i++) {
    - let rand = Math.floor(Math.random()*5)
    img(src=`/images/a${rand+1}.png` , alt=`a-${rand+1} image`, height="300", width="300", style="padding:2px")
  - }